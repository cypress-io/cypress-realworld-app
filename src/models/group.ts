//a group model we created
export interface Group {
    id: string;
    uuid: string;
    name: string;
    creatorId: string;
    createdAt: Date;
    modifiedAt: Date;
}

// *to-add - total amount,


  