version: '3'

services:
  frontend:
    build: .
    labels:
      shipyard.route: '/'
    ports:
      - '3000:3000'
    env_file:
      - .env
  # backend:
  #   build: .
  #   ports:
  #     - '3001:3001'
  #   env_file:
  #     - .env
  #   volumes:
  #     - './data:/data'


